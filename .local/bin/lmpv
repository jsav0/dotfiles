#!/bin/bash
SOCKET=/tmp/mpfifo

next() {
	echo "playlist-next" > $SOCKET
}

prev() {
	echo "playlist-prev" > $SOCKET
}

pause_play() {
	echo "keypress p" > $SOCKET
}

case $1 in
	next) next;;
	prev) prev;;
	p) pause_play;;
	*) echo usage;;
esac
